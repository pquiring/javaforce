<project name="jbus-client" default="executable" basedir=".">
  <description>jbus-client</description>
  <!-- set global properties for this build -->
  <property name="app" value="jbus-client"/>
  <property name="apptype" value="console"/>
  <property name="cfg" value="cfg/jbus-client.cfg"/>
  <property name="ico" value="javaforce.ico"/>
  <property name="home" value=".."/>

  <property environment="env"/>

  <import file="${home}/base.xml"/>

  <target name="depjars" depends="depnatives">
  </target>

  <target name="compile" depends="depjars" description="compile the source">
  </target>

  <target name="jar" depends="depjars,compile" description="build jar file">
  </target>

  <!-- sudo ant install -->
  <target name="install" description="install files">
  </target>
</project>
