These are example apps:

desktop-ui
 - simple desktop UI app

console
 - simple console app

service
 - service that runs in the background (Windows : service.msc, Linux : systemctl)

servlet
 - web app (Java Servlet)

servlet-websocket
 - web app (Java Servlet) with a simple WebSocket chat program
