[Unit]
Description=JavaForce Proxy Server
After=jfbusserver.service

[Service]
ExecStart=/usr/bin/jfproxy-server
ExecStop=/usr/bin/jfproxy-server --stop
Restart=always

[Install]
WantedBy=multi-user.target
