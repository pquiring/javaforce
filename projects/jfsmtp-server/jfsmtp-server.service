[Unit]
Description=JavaForce SMTP Server
After=jfbusserver.service

[Service]
ExecStart=/usr/bin/jfsmtp-server
ExecStop=/usr/bin/jfsmtp-server --stop
Restart=always

[Install]
WantedBy=multi-user.target
