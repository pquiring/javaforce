[Unit]
Description=JavaForce SSH Server
After=jfbusserver.service

[Service]
ExecStart=/usr/bin/jfssh-server
ExecStop=/usr/bin/jfssh-server --stop
Restart=always

[Install]
WantedBy=multi-user.target
