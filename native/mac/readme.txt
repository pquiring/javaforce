Mac Loader
==========

Desc
----

  - These C++ sources build the Mac loader and native API used by JavaForce
  - the native API provides features that are currently unavailable in Java

Requirements
------------

  export FFMPEG_HOME=$HOME/ffmpeg

  Install Xcode command line tools.  Run git from terminal and that will prompt to install them.

Compiling
---------
  ant
