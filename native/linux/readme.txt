Debian/Ubuntu:

  Required packages:
    deps.sh

  Compile:
    ant linux

Fedora:

  Required packages:
    deps.sh

  Compile:
    ant linux

Arch:

  Required packages :
    deps.sh

  Compile:
    ant linux

FreeBSD:

  Required packages :
    deps.sh

  Compile:
    ant freebsd

Raspberry Pi (Debian) (64bit):

  Required packages:
    deps.sh

  Compile:
    ant arm64
