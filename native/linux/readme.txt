Debian/Ubuntu:

  Required packages:
    chmod +x deps.sh
    sudo ./deps.sh

  Compile:
    ant linux

Fedora:

  Required packages:
    chmod +x deps.sh
    sudo ./deps.sh

  Compile:
    ant linux

Arch:

  Required packages :
    chmod +x deps.sh
    sudo ./deps.sh

  Compile:
    ant linux

FreeBSD:

  Required packages :
    chmod +x deps.sh
    sudo ./deps.sh

  Compile:
    ant freebsd

Raspberry Pi (Debian) (64bit):

  Required packages:
    chmod +x deps.sh
    sudo ./deps.sh

  Compile:
    ant arm64
