To generate the ISO first run ./repo/ubuntu/update.sh after building/install/packaging/uploading all projects.

Then come here and run jfLinux.sh to build the ISO.
Edit jfLinux.sh and change ARCH to i386 or x86_64 to build each if desired.
The generated ISO will be placed in ~/.work

